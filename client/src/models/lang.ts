export type LoginPageLanguage = {
    login: string
    email: string
    password: string
}
export type DashboardLanguage = {
    dashboard: string
    subjects: string
    courses: string
    profile: string
    exams: string
    questions: string
    teachers: string
    students: string
}
