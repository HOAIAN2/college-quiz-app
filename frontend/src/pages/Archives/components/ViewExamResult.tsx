type ViewExamResultProps = {
    id: string | number;
};

function ViewExamResult({
    id,
}: ViewExamResultProps) {
    // Actual view exam result
    console.log(id);
    return (
        <>
        </>
    );
}

export default ViewExamResult;